* Pi Coding Agent — Configs

Configurações para o [[https://pi.dev/][Pi Coding Agent]], assistente de código AI via CLI.

O Pi é um agente de codificação no terminal que suporta múltiplos providers (Ollama, llama.cpp/Serve, servers remotos), tool-calling, raciocínio (= :think =) e compação de contexto. Repositório: [[https://github.com/badlogic/pi-mono][github.com/badlogic/pi-mono]].

** Arquivos

| Arquivo              | Destino                        | Descrição                                  |
|----------------------+--------------------------------+--------------------------------------------|
| =models.json=        | =~/.pi/agent/models.json=      | Providers, modelos e compatibility flags   |
| =settings.json=      | =~/.pi/agent/settings.json=    | Modelo padrão, compação, pacotes           |
| =auth.json=          | =~/.pi/agent/auth.json=        | Credenciais — **NÃO commit**               |
| =mcp.json=           | =~/.pi/agent/mcp.json=         | Servidores MCP (DuckDuckGo, SearXNG)       |
| =extensions/latency= | =~/.pi/agent/extensions/=     | Extensão de medição de latência por modelo |

** Sync

Copiar do source of truth (ai-dotfiles) para o diretório runtime:

#+begin_src bash
# Configs principais
cp ~/git/ai-dotfiles/configs/pi/models.json   ~/.pi/agent/models.json
cp ~/git/ai-dotfiles/configs/pi/settings.json ~/.pi/agent/settings.json
cp ~/git/ai-dotfiles/configs/pi/mcp.json     ~/.pi/agent/mcp.json

# Extensão de latência (construir antes se necessário)
cp -r ~/git/ai-dotfiles/configs/pi/extensions ~/.pi/agent/extensions

# auth.json NÃO é sincronizado — contém secrets, gerenciar separadamente
#+end_src

** Providers

| Provider     | baseUrl                        | Porta  | Descrição                                  |
|--------------+--------------------------------+--------+--------------------------------------------|
| =ollama=     | =http://127.0.0.1:11434/v1=   |  11434 | Modelos cloud via Ollama                   |
| =llama-swap= | =http://127.0.0.1:12434/v1=   |  12434 | Modelos locais via llama-swap/llama.cpp    |
| =sisyphus=   | =http://192.168.3.8:8081/v1=  |   8081 | Modelos edge no servidor caseiro (Sisyphus)|

Notas:
- =ollama= usa =supportsDeveloperRole: false= e =supportsReasoningEffort: false= (API Ollama não suporta features OpenAI-specific)
- =llama-swap= usa =thinkingFormat: "qwen-chat-template"= (requer =chat_template_kwargs.enable_thinking= no llama.cpp)
- =sisyphus= não precisa de API key (=not-needed=)

** Modelos Cloud (Ollama) — 14 modelos

| Model ID                      | Nome                          | Contexto | Visão |
|-------------------------------+-------------------------------+----------+-------|
| =deepseek-v4-flash:cloud=      | DeepSeek V4 Flash          |      1M  | não   |
| =deepseek-v4-pro:cloud=        | DeepSeek V4 Pro            |      1M  | não   |
| =gemini-3-flash-preview:cloud= | Gemini 3 Flash Preview     |      1M  | sim   |
| =gemma4:31b-cloud=             | Gemma 4 31B                |    262K  | sim   |
| =glm-5.1:cloud=                | GLM 5.1                    |    202K  | não   |
| =glm-5:cloud=                  | GLM 5                      |    202K  | não   |
| =kimi-k2.6:cloud=              | Kimi K2.6                  |    262K  | sim   |
| =kimi-k2.5:cloud=              | Kimi K2.5                  |    262K  | sim   |
| =minimax-m2.5:cloud=           | MiniMax M2.5               |    205K  | não   |
| =minimax-m2.7:cloud=           | MiniMax M2.7 1M            |      1M  | não   |
| =minimax-m3:cloud=             | MiniMax M3                 |    524K  | sim   |
| =nemotron-3-super:cloud=       | Nemotron 3 Super           |    262K  | não   |
| =nemotron-3-nano:30b-cloud=    | Nemotron 3 Nano 30B 1M     |      1M  | não   |
| =qwen3.5:cloud=                | Qwen 3.5 397B              |    262K  | sim   |

** Modelo Locais (llama-swap) — 22 modelos base + 16 variantes :think

Todos os modelos do fleet estão disponíveis no Pi (exceção: modelo de embedding excluído).
Modelos com raciocínio têm variante =:think= que ativa modo de thinking.

*** Modelos com Tool-Calling

| Model ID                      | Nome                          | Contexto | Input          | :think |
|--------------------------------+-------------------------------+----------+----------------+--------|
| =gemma4-12b=                    | Gemma 4 12B                  |    262K  | text+image      | sim  |
| =gemma4-26b-a4b=               | Gemma 4 26B MoE              |    262K  | text           | sim    |
| =gemma4-e2b=                   | Gemma 4 E2B                  |    131K  | text           | sim    |
| =gemma4-e4b=                   | Gemma 4 E4B                  |    131K  | text           | sim    |
| =gpt-oss-20b=                  | GPT-OSS 20B ★                |    131K  | text           | sempre |
| =lfm2.5-8b-a1b=                | LFM 2.5 8B MoE               |    128K  | text           | sim    |
| =littlelamb-0.3b-tc=           | LittleLamb 0.3B TC           |     40K  | text           | sim    |
| =mellum2-12b-a2.5b=            | Mellum 2 12B MoE             |    131K  | text           | sim    |
| =qwen3.5-4b=                   | Qwen3.5 4B                   |    262K  | text           | sim    |
| =qwen3.5-4b-abliterated=       | Qwen3.5 4B Abliterated       |    262K  | text           | sim    |
| =qwen3.5-9b=                   | Qwen3.5 9B                   |    262K  | text           | sim    |
| =qwen3.6-35b-a3b=              | Qwen 3.6 35B MoE             |    262K  | text           | sim    |
| =qwopus-coder-9b=              | Qwopus 9B Coder              |    262K  | text           | sim    |

*** Modelos Multimodal (Visão/Audio)

| Model ID                      | Nome                          | Contexto | Input          | :think |
|--------------------------------+-------------------------------+----------+----------------+--------|
| =gemma4-12b=                    | Gemma 4 12B                  |    262K  | text+image      | sim  |
| =glm-ocr=                      | GLM-OCR                      |    131K  | text+image     | não    |
| =lfm2.5-vl-1.6b-extract=       | LFM2.5-VL 1.6B Extract       |    128K  | text+image     | não    |
| =lfm2.5-vl-450m=               | LFM2.5-VL 450M               |    128K  | text+image     | não    |
| =minicpm-v-4.6=                | MiniCPM-V 4.6                 |    262K  | text+image     | sim    |

*** Modelos de Propósito Específico (sem tool-calling)

| Model ID                      | Nome                          | Contexto | :think |
|--------------------------------+-------------------------------+----------+--------|
| =hy-mt2-1.8b=                  | Hy-MT2 1.8B                  |    262K  | não    |
| =translategemma-4b=            | TranslateGemma 4B             |    131K  | não    |
| =webworld-8b=                  | WebWorld 8B                   |     40K  | não    |

*** Variante :think

Modelos com raciocínio toggável têm variante =:think= que ativa modo de thinking. Nome exibido com emoji 🧠.
Exemplo: =gemma4-12b:think= → "🧠 Gemma 4 12B Thinking"

Modelos marcados com ★ são /always thinking/ — sempre geram raciocínio, sem toggle. Não têm variante =:think=.
- =gpt-oss-20b= (Harmony format)

** Modelos Edge (Sisyphus) — 3 modelos

| Model ID        | Nome              | Contexto | Preço |
|-----------------+-------------------+----------+-------|
| =qwen3-1.7b=    | Qwen3 1.7B 📶    |     16K  | grátis |
| =qwen3-0.6b=    | Qwen3 0.6B 📶    |     16K  | grátis |
| =gemma3-1b=     | Gemma 3 1B 📶    |      8K  | grátis |

Servidor caseiro em =192.168.3.8:8081=. Modelos pequenos para testes rápidos.

** Configuração MCP

Servidores MCP configurados em =mcp.json=:

*** DuckDuckGo

#+begin_src json
{
  "duckduckgo": {
    "command": "uvx",
    "args": ["duckduckgo-mcp-server"],
    "lifecycle": "lazy",
    "idleTimeout": 10,
    "directTools": true
  }
}
#+end_src

- Usa =uvx duckduckgo-mcp-server= (instala via =pip install uvx=)
- =lifecycle: lazy= — sobe só quando necessário
- =idleTimeout: 10= — desliga após 10 min sem uso
- =directTools: true= — ferramentas disponíveis diretamente no Pi

*** SearXNG

#+begin_src json
{
  "searxng": {
    "command": "npx",
    "args": ["-y", "mcp-searxng"],
    "env": { "SEARXNG_URL": "http://192.168.3.8:8888" },
    "lifecycle": "lazy",
    "idleTimeout": 10
  }
}
#+end_src

- Usa =npx -y mcp-searxng= (executa sem instalação global)
- Aponta para instância SearXNG em =http://192.168.3.8:8888=
- =lifecycle: lazy= e =idleTimeout: 10=

** Compação de Contexto

Configuração recomendada em =settings.json= para modelos locais:

#+begin_src json
{
  "compaction": {
    "enabled": true,
    "reserveTokens": 16384,
    "keepRecentTokens": 20000
  }
}
#+end_src

- =reserveTokens: 16384= — reserva espaço suficiente para respostas longas (+ raciocínio em modelos =:think=)
- =keepRecentTokens: 20000= — mantém as mensagens mais recentes intactas durante compação
- Modelo padrão atual: =qwen3.6-35b-a3b:think= (provider =llama-swap=)

** Uso

#+begin_src bash
# Usar modelo cloud
pi --model glm-5.1:cloud

# Usar modelo local (provider padrão já é llama-swap)
pi --model qwen3.6-35b-a3b

# Usar modelo local com raciocínio
pi --model "qwen3.6-35b-a3b:think"

# Usar modelo local multimodal (visão + áudio)
pi --model gemma4-12b

# Usar modelo edge no Sisyphus
pi --provider sisyphus --model qwen3-1.7b

# Listar todos os modelos disponíveis
pi --list-models
#+end_src

** Armadilhas (Pitfalls)

1. *O campo é =baseUrl=, não =baseURL=*. O Pi usa camelCase (=baseUrl=) diferente do padrão OpenAI (=baseURL=). Se usar =baseURL=, o provider não funciona.

2. *=models= deve ser um array*. Cada provider precisa de ="models": [...]= com objetos contendo pelo menos ="id"=, ="name"=, ="reasoning"=, ="contextWindow"= e ="maxTokens"=. String avulsa não funciona.

3. *=PATH= deve incluir =~/.hermes/node/bin=. O Pi instala o runtime Node.js em =~/.hermes/node/bin=. Se o =$PATH= não incluir esse diretório, extensões e servidores MCP via =npx= falham silenciosamente.

4. *Compatibilidade de tool-calling*. Modelos locais pequenos (< 7B) podem não suportar tool-calling corretamente. O Pi tenta adaptar, mas modelos como =littlelamb-0.3b-tc= são exceções especializadas.

5. *Não instalar via AUR*. O pacote =pi-coding-agent= no AUR está frequentemente desatualizado e pode quebrar configs. Instalar sempre via =npm= ou =pnpm= global.

6. *=auth.json= contém secrets* — **NUNCA** commitar no git. Manter fora do repositório.

7. *=llama-swap= é o nome do provider* (não =local= nem =llama.cpp=). Deve bater com a chave em =models.json=.

8. *=thinkingFormat: "qwen-chat-template"=* no provider =llama-swap= é obrigatório para modelos =:think=. O llama.cpp requer =chat_template_kwargs.enable_thinking= no chat template.

9. *=supportsDeveloperRole: false= e =supportsReasoningEffort: false=* no provider =ollama= são necessários porque a API Ollama não implementa essas features do spec OpenAI.

10. *Modelos com vision/audio*. O campo =input= suporta ="text"= e ="image"= (envio de imagem via =@caminho/arquivo.png= ou clipboard). O Pi **não suporta ="audio"= no schema** — mesmo que o modelo tenha capacidade de áudio (como o Gemma 4 12B), use apenas ="text", ="image"=.

11. *Pi recarrega =models.json= automaticamente* ao abrir o seletor de modelos (= /model = no TUI) — não é necessário reiniciar.