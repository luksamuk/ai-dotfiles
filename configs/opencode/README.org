#+TITLE: OpenCode configuration

Some configuration for OpenCode.

- ~opencode.json~: Basic  configuration for OpenCode.   Includes plans that  I run
  locally.
- ~AGENTS.template.md~: Some  extra snippets for  an eventual ~AGENTS.md~  file that
  might come in handy.

