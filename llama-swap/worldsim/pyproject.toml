[project]
name = "worldsim"
version = "0.1.0"
description = "Web world model simulation — Agent + World Model loop via llama-swap"
requires-python = ">=3.10"
dependencies = [
    "rich>=13.0",
    "questionary>=2.0",
    "requests>=2.28",
]