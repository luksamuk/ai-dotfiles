healthCheckTimeout: 300
logLevel: info
logTimeFormat: rfc3339
logToStdout: proxy
captureBuffer: 15
startPort: 10001
sendLoadingState: false
includeAliasesInList: true
globalTTL: 0
macros:
  models_dir: ${env.HOME}/.llama-models
  media_path: ${env.HOME}/testfiles/vision
  llama_server: ${env.HOME}/git/llama.cpp/build/bin/llama-server
  ik_llama_server: ${env.HOME}/git/ik_llama.cpp/build/bin/llama-server
  bee_server: ${env.HOME}/git/beellama.cpp/build/bin/llama-server
  vllm_bin: ${env.HOME}/.vllm/bin/vllm
  default_temp: '0.7'
  default_top_p: '0.9'
  default_top_k: '20'
  default_min_p: '0.01'
  default_repeat_penalty: '1.05'
  code_temp: '0.6'
  code_top_p: '0.85'
  code_top_k: '40'
  code_min_p: '0.02'
  code_repeat_penalty: '1.05'
  think_temp: '0.6'
  think_top_p: '0.9'
  think_top_k: '20'
  think_min_p: '0.0'
  think_repeat_penalty: '1.0'
  small_model_min_ctx: '65536'
  small_model_max_ctx: '131072'
  large_model_min_ctx: '65536'
  large_model_max_ctx: '131072'
  vram_margin: '512'
  small_cache_k: q8_0
  small_cache_v: q8_0
  large_cache_k: q4_0
  large_cache_v: q4_0
  gemma_cache_k: q4_0
  gemma_cache_v: q4_0
models:
  qwen3.5-4b:
    name: Qwen3.5 4B
    description: Qwen 3.5 is a family of open-source multimodal models that delivers exceptional utility and performance, integrating breakthroughs in multimodal learning, architectural efficiency, and reinforcement learning.
    cmd: '${bee_server} --port ${PORT}

      --model ${models_dir}/Qwen3.5-4B-abliterated.i1-Q4_K_M.gguf

      --spec-draft-model ${models_dir}/qwen35-4b-dflash-Q4_K_M.gguf

      --spec-type dflash

      --spec-draft-ngl all

      --spec-dflash-cross-ctx 512

      --spec-draft-n-max 8

      --spec-branch-budget 0

      --jinja

      --parallel 1

      -ngl all

      --ctx-size 32768

      --temp ${default_temp}

      --top-p ${default_top_p}

      --top-k ${default_top_k}

      --min-p ${default_min_p}

      --repeat-penalty ${default_repeat_penalty}

      --reasoning on

      --cache-type-k q4_0

      --cache-type-v q4_0

      --flash-attn on

      --metrics

      '
    filters:
      stripParams: temperature, top_p
      setParamsByID:
        ${MODEL_ID}:
          chat_template_kwargs:
            enable_thinking: false
          temperature: 0.7
          top_p: 0.9
        ${MODEL_ID}:think:
          chat_template_kwargs:
            enable_thinking: true
          temperature: 0.6
          top_p: 0.9
    ttl: 60
    health_check_timeout: 300
    capabilities:
      in:
      - text
      out:
      - text
      tools: true
      reranker: false
      context: 32768
    metadata:
      size: ~2.9 GB (Q4_K_M + DFlash drafter)
      context: '32K (DFlash max on 6GB: 4782 MiB. 98K needs 5678 MiB — doesn''t fit even alone)'
      vram_usage: ~4.8GB VRAM (model 2.6GB + drafter 313MB + DFlash ring + q4_0 cache 32K)
      source: Qwen3.5-4B-abliterated i1-Q4_K_M + Anbeeld/Qwen3.5-4B-DFlash-GGUF
      kv_cache: q4_0
      features:
        completion: true
        tools: true
        thinking: true
        vision: false
        embedding: false
        insert: false
        audio: false
        image: false
      architecture: qwen35
      parameters: 4210000000
      context_length: 32768
      embedding_length: 2560
      quantization: i1-Q4_K_M + DFlash Q4_K_M
      note: DFlash speculative decoding via BeeLlama. 1.64x faster than ik+MoQ (86 vs 53 tok/s). Flat DFlash (branch-budget=0), adaptive draft-max (profit controller). Tool calling + thinking tested OK.
  qwen3.5-9b:
    name: Qwen3.5 9B
    description: Qwen 3.5 is a family of open-source multimodal models that delivers exceptional utility and performance, integrating breakthroughs in multimodal learning, architectural efficiency, and reinforcement learning.
    cmd: '${ik_llama_server} --port ${PORT}

      --model ${models_dir}/Qwen3.5-9B-MoQ-3.6.gguf

      --fit

      --fit-margin 512

      --jinja

      --parallel 2

      --parallel-tool-calls

      --ctx-size ${small_model_max_ctx}

      --temp ${default_temp}

      --top-p ${default_top_p}

      --top-k ${default_top_k}

      --min-p ${default_min_p}

      --repeat-penalty ${default_repeat_penalty}

      --reasoning on

      --cache-type-k q8_0

      --cache-type-v q4_0

      --k-cache-hadamard

      --v-cache-hadamard

      --flash-attn auto

      --metrics

      '
    filters:
      stripParams: temperature, top_p
      setParamsByID:
        ${MODEL_ID}:
          chat_template_kwargs:
            enable_thinking: false
          temperature: 0.7
          top_p: 0.9
        ${MODEL_ID}:think:
          chat_template_kwargs:
            enable_thinking: true
          temperature: 0.6
          top_p: 0.9
    ttl: 60
    health_check_timeout: 300
    capabilities:
      in:
      - text
      out:
      - text
      tools: true
      reranker: false
      context: 131072
    metadata:
      size: ~3.75 GB (MoQ-3.6)
      context: 64K-128K (dynamic)
      vram_usage: ~4.8GB VRAM (text-only, ik with hadamard cache)
      source: w-ahmad/Qwen3.5-9B-GGUF-MoQ (MoQ-3.6)
      kv_cache: q8_0 + q4_0 hadamard
      features:
        completion: true
        tools: true
        thinking: true
        vision: false
        embedding: false
        insert: false
        audio: false
        image: false
      architecture: qwen35
      parameters: 8950000000
      context_length: 262144
      embedding_length: 4096
      quantization: MoQ-3.6
      note: 'MoQ-3.6 replaces UD-Q4_K_XL: same speed (12.2 tok/s), equal+ quality (21/25 vs 20/25 Snake), 32% smaller (3.75 vs 5.55 GB). MTP OOM on 6GB VRAM.'
  gemma4-e4b:
    name: Gemma 4 E4B
    description: Gemma 4 models are designed to deliver frontier-level performance at each size. They are well-suited for reasoning, agentic workflows, coding, and multimodal understanding.
    cmd: '${ik_llama_server} --port ${PORT}

      --model ${models_dir}/gemma-4-E4B-it-Q4_K_M.gguf

      # No --no-mmproj: ik doesn''t support this flag (upstream-only)

      --fit

      --fit-margin 512

      --jinja

      --parallel 2

      --parallel-tool-calls

      --ctx-size ${small_model_max_ctx}

      --temp 0.7

      --top-p 0.9

      --top-k 40

      --min-p 0.01

      --repeat-penalty 1.0

      --reasoning on

      --cache-type-k q8_0

      --cache-type-v q4_0

      --k-cache-hadamard

      --v-cache-hadamard

      --flash-attn auto

      --metrics

      '
    filters:
      stripParams: temperature, top_p
      setParamsByID:
        ${MODEL_ID}:
          chat_template_kwargs:
            enable_thinking: false
          temperature: 0.7
          top_p: 0.9
        ${MODEL_ID}:think:
          chat_template_kwargs:
            enable_thinking: true
          temperature: 0.6
          top_p: 0.9
    ttl: 60
    health_check_timeout: 300
    capabilities:
      in:
      - text
      out:
      - text
      tools: true
      reranker: false
      context: 131072
    metadata:
      size: ~4.63 GB (Q4_K_M)
      context: 64K-128K (dynamic)
      vram_usage: ~5.3GB VRAM (fits with mmproj)
      source: unsloth/gemma-4-E4B-it-GGUF (Q4_K_M)
      kv_cache: q8_0 + q4_0 hadamard
      features:
        completion: true
        tools: true
        thinking: true
        vision: false
        embedding: false
        insert: false
        audio: false
        image: false
      architecture: gemma4
      parameters: 7520000000
      context_length: 131072
      embedding_length: 2560
      quantization: Q4_K_M
      note: Upgraded from UD-Q3_K_XL → Q4_K_M. Text-only (no mmproj). Tightest VRAM model — adding mmproj reduces ctx.
  gemma4-e2b:
    name: Gemma 4 E2B
    description: Gemma 4 E2B QAT — Unsloth Dynamic 2.0 (UD-Q4_K_XL). Higher accuracy and smaller than Google Q4_0.
    cmd: '${llama_server} --port ${PORT}

      --model ${models_dir}/gemma-4-E2B-it-qat-UD-Q4_K_XL.gguf

      --no-mmproj

      --fit on

      --fit-target 1024

      --fit-ctx ${small_model_min_ctx}

      --ctx-size ${small_model_max_ctx}

      --parallel 4

      --temp 0.7

      --top-p 0.9

      --top-k 40

      --min-p 0.01

      --repeat-penalty 1.0

      --reasoning on

      --cache-type-k ${gemma_cache_k}

      --cache-type-v ${gemma_cache_v}

      --flash-attn on

      --metrics

      '
    filters:
      stripParams: temperature, top_p
      setParamsByID:
        ${MODEL_ID}:
          chat_template_kwargs:
            enable_thinking: false
          temperature: 0.7
          top_p: 0.9
        ${MODEL_ID}:think:
          chat_template_kwargs:
            enable_thinking: true
          temperature: 0.6
          top_p: 0.9
    ttl: 60
    health_check_timeout: 300
    capabilities:
      in:
      - text
      out:
      - text
      tools: true
      reranker: false
      context: 131072
    metadata:
      size: ~2.5 GB (UD-Q4_K_XL)
      context: 32K-128K (dynamic)
      vram_usage: ~2.5GB
      source: unsloth/gemma-4-E2B-it-qat-GGUF
      kv_cache: q4_0 + attn_rot
      features:
        completion: true
        tools: true
        thinking: true
        vision: false
        embedding: false
        insert: false
        audio: false
        image: false
      architecture: gemma4
      parameters: 4650000000
      context_length: 131072
      embedding_length: 1536
      quantization: UD-Q4_K_XL
      note: Unsloth Dynamic 2.0 QAT. Better accuracy than Google Q4_0 (Top-1 98.16% vs 89.29%), 22% smaller. Upstream only — NOT compatible with ik_llama.
  lfm2.5-vl-450m:
    name: LFM2.5-VL 450M
    description: Edge vision-language model for image understanding, object detection, and captioning using LFM backbones and custom visual encoders.
    cmd: '${llama_server} --port ${PORT}

      --model ${models_dir}/LFM2.5-VL-450M-Q8_0.gguf

      --jinja

      --mmproj ${models_dir}/mmproj-LFM2.5-VL-450m-F16.gguf

      --media-path ${media_path}

      --fit on

      --fit-target ${vram_margin}

      --fit-ctx ${small_model_min_ctx}

      --ctx-size ${small_model_max_ctx}

      --temp 0.1

      --top-p 0.9

      --top-k 40

      --min-p 0.15

      --repeat-penalty 1.05

      --reasoning off

      --cache-type-k f16

      --cache-type-v f16

      --flash-attn on

      --metrics

      '
    health_check_timeout: 300
    ttl: 60
    capabilities:
      in:
      - text
      - image
      out:
      - text
      tools: false
      reranker: false
      context: 131072
    metadata:
      size: 0.36 GB (Q8_0) + 0.18 GB mmproj
      context: 32K-128K (dynamic)
      vram_usage: ~0.5GB
      source: LiquidAI/LFM2.5-VL-450M-GGUF
      kv_cache: f16
      features:
        completion: true
        tools: false
        thinking: false
        vision: true
        embedding: false
        insert: false
        audio: false
        image: false
      architecture: lfm2
      parameters: 354480000
      context_length: 128000
      embedding_length: 1024
      quantization: Q8_0
  qwen3.6-35b-a3b:
    name: Qwen 3.6 35B
    description: Qwen3.6 delivers substantial upgrades in agentic coding and thinking preservation. Built on direct community feedback for a more intuitive, responsive, and productive coding experience.
    cmd: '${ik_llama_server} --port ${PORT}

      --model ${models_dir}/Qwen3.6-35B-A3B-APEX-I-Compact.gguf

      # No --mmproj: vision crashes on 6GB RTX 3050 (CLIP compute graph OOM). Text-only = stable 33-34 t/s.

      # No --fit-ctx: ik_llama.cpp doesn''t support this flag; --ctx-size serves as ceiling

      --fit

      --fit-margin 768

      --ctx-size 131072

      --temp ${code_temp}

      --top-p ${code_top_p}

      --top-k ${code_top_k}

      --min-p ${code_min_p}

      --repeat-penalty ${code_repeat_penalty}

      --reasoning on

      --jinja

      --parallel 1

      --parallel-tool-calls

      --cache-type-k ${large_cache_k}

      --cache-type-v ${large_cache_v}

      --k-cache-hadamard

      --v-cache-hadamard

      --flash-attn auto

      --defer-experts

      --metrics

      '
    filters:
      stripParams: temperature, top_p
      setParamsByID:
        ${MODEL_ID}:
          chat_template_kwargs:
            enable_thinking: false
          temperature: 0.6
          top_p: 0.85
        ${MODEL_ID}:think:
          chat_template_kwargs:
            enable_thinking: true
          temperature: 0.6
          top_p: 0.9
    ttl: 300
    health_check_timeout: 300
    capabilities:
      in:
      - text
      out:
      - text
      tools: true
      reranker: false
      context: 131072
    metadata:
      size: ~17.3 GB (APEX I-Compact)
      context: 1K-131K (safe max; 200K works but zero VRAM headroom)
      vram_usage: ~5.4GB VRAM + 13.8GB pinned RAM (ik pinned memory, stable 33-34 t/s up to 131K)
      source: mudler/Qwen3.5-35B-A3B-APEX-GGUF
      kv_cache: q4_0 + attn_rot + hadamard
      features:
        completion: true
        tools: true
        thinking: true
        vision: false
        embedding: false
        insert: false
        audio: false
        image: false
      architecture: qwen35moe
      parameters: 34660000000
      context_length: 262144
      embedding_length: 2048
      quantization: I-Compact
      note: Vision blocked — mmproj loads fine (858 MiB F16, ~28 t/s text) but CLIP compute graph OOMs on 6GB RTX 3050 at inference time. Reverted to text-only (33-34 t/s, stable).
  ornith-1.0-35b:
    name: Ornith 1.0 35B
    description: Ornith-1.0 is a family of open-source LLMs specialized for agentic coding, trained with a novel self-scaffolding reinforcement learning approach that jointly optimizes the scaffold and the solution.
    cmd: '${ik_llama_server} --port ${PORT}

      --model ${models_dir}/ornith-1.0-35b/Ornith-1.0-35B-APEX-I-Compact.gguf

      --fit

      --fit-margin 768

      --ctx-size 131072

      --temp 1.0

      --top-p 1.0

      --top-k ${code_top_k}

      --min-p ${code_min_p}

      --repeat-penalty ${code_repeat_penalty}

      --reasoning on

      --jinja

      --parallel 1

      --parallel-tool-calls

      --cache-type-k ${large_cache_k}

      --cache-type-v ${large_cache_v}

      --k-cache-hadamard

      --v-cache-hadamard

      --flash-attn auto

      --defer-experts

      --metrics

      '
    filters:
      stripParams: temperature, top_p
      setParamsByID:
        ${MODEL_ID}:
          chat_template_kwargs:
            enable_thinking: false
          temperature: 1.0
          top_p: 1.0
        ${MODEL_ID}:think:
          chat_template_kwargs:
            enable_thinking: true
          temperature: 1.0
          top_p: 1.0
    ttl: 300
    health_check_timeout: 300
    capabilities:
      in:
      - text
      out:
      - text
      tools: true
      reranker: false
      context: 131072
    metadata:
      size: ~16.5 GB (APEX I-Compact)
      context: 1K-131K (safe max)
      vram_usage: ~5.4GB VRAM + ~12GB pinned RAM (ik pinned memory, estimated 33-34 t/s)
      source: mudler/Ornith-1.0-35B-APEX-GGUF
      kv_cache: q4_0 + attn_rot + hadamard
      features:
        completion: true
        tools: true
        thinking: true
        vision: false
        embedding: false
        insert: false
        audio: false
        image: false
      architecture: qwen35moe
      parameters: 35000000000
      context_length: 262144
      embedding_length: 2048
      quantization: I-Compact
      note: 'Post-trained Qwen 3.5 35B MoE. Self-scaffolding RL for agentic coding. 256 experts, 8+1 active, ~3B active/token. APEX I-Compact: 22% smaller than Q4_K_M, imatrix v1.3 calibrated. Expected 33-34 t/s matching Qwen 3.6 APEX.'
  nex-n2-mini:
    name: Nex N2 Mini
    description: Nex-N2-mini is a 35B Mixture-of-Experts agentic model with Agentic Thinking, post-trained on Qwen3.5-35B-A3B-Base for first-tier coding and long-horizon agentic tasks. Features adaptive thinking depth and coherent reasoning across diverse agentic workflows.
    cmd: '${ik_llama_server} --port ${PORT}

      --model ${models_dir}/nex-n2-mini/Nex-N2-mini-APEX-I-Compact.gguf

      # No --mmproj: vision crashes on 6GB RTX 3050 (CLIP compute graph OOM). Text-only = stable.

      --fit

      --fit-margin 768

      --ctx-size 131072

      --temp 0.7

      --top-p 0.95

      --top-k 40

      --min-p 0.0

      --repeat-penalty 1.0

      --presence-penalty 1.1

      --reasoning on

      --jinja

      --parallel 1

      --parallel-tool-calls

      --cache-type-k ${large_cache_k}

      --cache-type-v ${large_cache_v}

      --k-cache-hadamard

      --v-cache-hadamard

      --flash-attn auto

      --defer-experts

      --metrics

      '
    filters:
      stripParams: temperature, top_p
      setParamsByID:
        ${MODEL_ID}:
          chat_template_kwargs:
            enable_thinking: false
          temperature: 0.7
          top_p: 0.95
        ${MODEL_ID}:think:
          chat_template_kwargs:
            enable_thinking: true
          temperature: 0.7
          top_p: 0.95
    ttl: 300
    health_check_timeout: 300
    capabilities:
      in:
      - text
      out:
      - text
      tools: true
      reranker: false
      context: 131072
    metadata:
      size: ~16.5 GB (APEX I-Compact)
      context: 1K-131K (safe max)
      vram_usage: ~5.4GB VRAM + ~12GB pinned RAM (ik pinned memory, estimated 33-34 t/s)
      source: mudler/Nex-N2-mini-APEX-GGUF
      kv_cache: q4_0 + attn_rot + hadamard
      features:
        completion: true
        tools: true
        thinking: true
        vision: false
        embedding: false
        insert: false
        audio: false
        image: false
      architecture: qwen35moe
      parameters: 35000000000
      context_length: 262144
      embedding_length: 2048
      quantization: I-Compact
      note: '35B MoE agentic model. Qwen3.5 35B-A3B-Base. Agentic Thinking framework (Adaptive + Coherent). SWE-bench 74.4, Terminal-Bench 60.7. APEX I-Compact: imatrix v1.3 calibrated. Same arch/size as Qwen3.6/Ornith/Agents-A1 APEX — expects 33-34 t/s. Vision blocked on 6GB RTX 3050 (mmproj 903MB exists but CLIP OOMs at inference).'
  minicpm-v-4.6:
    name: MiniCPM-V 4.6
    description: A series of multimodal LLMs designed for vision-language understanding, with strong OCR capability, multi-image understanding, and efficient visual token processing.
    tags:
    - vision
    cmd: "${llama_server} --port ${PORT}\n--model ${models_dir}/MiniCPM-V-4_6-Q5_K_M.gguf\n--mmproj ${models_dir}/mmproj-MiniCPM-V-4.6-F16.gguf\n--media-path ${media_path}\n--jinja\n--fit on\n--fit-ctx 8192\n--ctx-size 262144\n  --parallel 4\n--cont-batching\n--temp ${default_temp}\n--top-p 0.8\n--top-k 100\n--min-p ${default_min_p}\n--repeat-penalty 1.05\n--reasoning on\n--cache-type-k f16\n--cache-type-v f16\n--flash-attn on\n--metrics\n"
    filters:
      stripParams: temperature, top_p
      setParamsByID:
        ${MODEL_ID}:
          chat_template_kwargs:
            enable_thinking: false
          temperature: 0.7
          top_p: 0.9
        ${MODEL_ID}:think:
          chat_template_kwargs:
            enable_thinking: true
          temperature: 0.6
          top_p: 0.9
    ttl: 60
    health_check_timeout: 300
    capabilities:
      in:
      - text
      - image
      out:
      - text
      tools: false
      reranker: false
      context: 262144
    metadata:
      size: 0.55 GB (Q5_K_M) + 1.1 GB mmproj
      context: 8K-256K (dynamic)
      vram_usage: ~2GB (model + mmproj + cache)
      source: openbmb/MiniCPM-V-4.6-gguf
      kv_cache: f16
      features:
        completion: true
        tools: false
        thinking: false
        vision: true
        embedding: false
        insert: false
        audio: false
        image: false
      architecture: qwen35
      parameters: 752160000
      context_length: 262144
      embedding_length: 1024
      quantization: Q5_K_M
  agents-a1-35b:
    name: Agents-A1 35B
    description: Agents-A1 is a 35B Mixture-of-Experts agentic model built to scale heterogeneous agentic abilities across long-horizon search, engineering, scientific research, instruction following, and tool calling.
    cmd: '${ik_llama_server} --port ${PORT}

      --model ${models_dir}/agents-a1-35b/Agents-A1-APEX-I-Compact.gguf

      # No --mmproj: vision crashes on 6GB RTX 3050 (CLIP compute graph OOM). Text-only = stable.

      --fit

      --fit-margin 768

      --ctx-size 131072

      --temp 0.85

      --top-p 0.95

      --top-k 20

      --min-p 0.0

      --repeat-penalty 1.0

      --presence-penalty 1.1

      --reasoning on

      --jinja

      --parallel 1

      --parallel-tool-calls

      --cache-type-k ${large_cache_k}

      --cache-type-v ${large_cache_v}

      --k-cache-hadamard

      --v-cache-hadamard

      --flash-attn auto

      --defer-experts

      --metrics

      '
    filters:
      stripParams: temperature, top_p
      setParamsByID:
        ${MODEL_ID}:
          chat_template_kwargs:
            enable_thinking: false
          temperature: 0.85
          top_p: 0.95
        ${MODEL_ID}:think:
          chat_template_kwargs:
            enable_thinking: true
          temperature: 0.85
          top_p: 0.95
    ttl: 300
    health_check_timeout: 300
    capabilities:
      in:
      - text
      out:
      - text
      tools: true
      reranker: false
      context: 131072
    metadata:
      size: ~16.5 GB (APEX I-Compact)
      context: 1K-131K (safe max)
      vram_usage: ~5.4GB VRAM + ~12GB pinned RAM (ik pinned memory, estimated 33-34 t/s)
      source: mudler/Agents-A1-APEX-GGUF
      kv_cache: q4_0 + attn_rot + hadamard
      features:
        completion: true
        tools: true
        thinking: true
        vision: false
        embedding: false
        insert: false
        audio: false
        image: false
      architecture: qwen35moe
      parameters: 35000000000
      context_length: 262144
      embedding_length: 2048
      quantization: I-Compact
      note: '35B MoE agentic model. Qwen3.5 35B-A3B base. Focused on long-horizon search, engineering, scientific research, tool calling. APEX I-Compact: imatrix v1.3 calibrated. Same arch/size as Qwen3.6/Ornith APEX — expects 33-34 t/s. Vision blocked on 6GB RTX 3050.'
  glm-4.7-flash:
    name: GLM 4.7 Flash
    description: GLM-4.7-Flash is a 30B Mixture-of-Experts model with Multi-head Latent Attention (MLA), 64 experts with 4 active per token. Built for fast, efficient conversational AI and tool calling.
    cmd: '${llama_server} --port ${PORT}

      --model ${models_dir}/glm-4.7-flash/GLM-4.7-Flash-APEX-I-Compact.gguf

      --fit on

      --fit-target 128

      --ctx-size 131072

      --temp 0.7

      --top-p 1.0

      --top-k -1

      --min-p 0.01

      --repeat-penalty 1.0

      --jinja

      --parallel 1

      --cache-type-k q4_0

      --cache-type-v q4_0

      --flash-attn on

      --metrics

      '
    filters:
      stripParams: temperature, top_p
      setParamsByID:
        ${MODEL_ID}:
          chat_template_kwargs:
            enable_thinking: false
          temperature: 1.0
          top_p: 0.95
        ${MODEL_ID}:think:
          chat_template_kwargs:
            enable_thinking: true
          temperature: 0.7
          top_p: 1.0
    ttl: 300
    health_check_timeout: 300
    capabilities:
      in:
      - text
      out:
      - text
      tools: true
      reranker: false
      context: 131072
    metadata:
      size: ~14.6 GB (APEX I-Compact)
      context: 1K-131K (MLA enables full context, 24 t/s at 131K)
      vram_usage: ~5GB VRAM + ~10GB pinned RAM (upstream llama.cpp, 30 tok/s at 128K with q4_0+FA)
      source: mudler/GLM-4.7-Flash-APEX-GGUF
      kv_cache: q4_0
      features:
        completion: true
        tools: true
        thinking: true
        vision: false
        embedding: false
        insert: false
        audio: false
        image: false
      architecture: deepseek2
      parameters: 30000000000
      context_length: 131072
      embedding_length: 2048
      quantization: I-Compact
      note: 30B MoE with MLA (Multi-head Latent Attention, DeepSeek-V2 style). 64 experts, 4 active, ~2B active/token. Smallest 35B-class in fleet (14.6GB). MLA compresses KV cache internally — no hadamard transform needed. Apache-2.0.
  glm-ocr:
    name: GLM-OCR
    description: Multimodal OCR model for complex document understanding, built on the GLM-V encoder-decoder architecture integrating CogViT visual encoder with a GLM-0.5B language decoder.
    cmd: '${llama_server} --port ${PORT}

      --model ${models_dir}/GLM-OCR-Q8_0.gguf

      --mmproj ${models_dir}/mmproj-GLM-OCR-Q8_0.gguf

      --jinja

      --media-path ${media_path}

      --fit on

      --fit-target ${vram_margin}

      --fit-ctx ${small_model_min_ctx}

      --ctx-size ${small_model_max_ctx}

      --temp 0.1

      --top-p 0.9

      --top-k 40

      --repeat-penalty 1.0

      --reasoning off

      --cache-type-k f16

      --cache-type-v f16

      --flash-attn on

      --metrics

      '
    ttl: 60
    capabilities:
      in:
      - text
      - image
      out:
      - text
      tools: false
      reranker: false
      context: 131072
    metadata:
      architecture: glm4
      parameters: 940000000
      context_length: 131072
      embedding_length: 2048
      quantization: Q8_0
      size: ~0.95 GB (Q8_0) + ~0.48 GB mmproj (Q8_0)
      vram_usage: ~2GB (model + mmproj + cache)
      source: ggml-org/GLM-OCR-GGUF (Q8_0 + mmproj Q8_0)
      kv_cache: f16
      features:
        completion: true
        tools: false
        thinking: false
        vision: true
        embedding: false
        insert: false
        audio: false
        image: false
      note: Specialized OCR model — use prompts like 'OCR', 'OCR markdown', 'Table Recognition:'. NOT a general VLM. Complements LFM2.5-VL-450M (general vision) for document extraction.
  hy-mt2-1.8b:
    name: Hy-MT2 1.8B
    description: Fast-thinking multilingual translation model from Tencent Hunyuan, supporting translation across 33 languages for complex real-world scenarios.
    cmd: '${llama_server} --port ${PORT}

      --model ${models_dir}/Hy-MT2-1.8B-Q4_K_M.gguf

      --jinja

      --fit on

      --fit-target 768

      --fit-ctx ${small_model_min_ctx}

      --ctx-size ${small_model_max_ctx}

      --parallel 4

      --temp 0.7

      --top-p 0.6

      --top-k 20

      --repeat-penalty 1.05

      --cache-type-k ${small_cache_k}

      --cache-type-v ${small_cache_v}

      --flash-attn on

      --metrics

      '
    health_check_timeout: 300
    ttl: 60
    capabilities:
      in:
      - text
      out:
      - text
      tools: false
      reranker: false
      context: 131072
    metadata:
      size: ~1.1 GB (Q4_K_M)
      context: 32K-128K (dynamic)
      vram_usage: ~2GB (model + cache)
      source: tencent/Hy-MT2-1.8B-GGUF (Q4_K_M)
      kv_cache: q8_0 + attn_rot
      features:
        completion: true
        tools: false
        thinking: false
        vision: false
        embedding: false
        insert: false
        audio: false
        image: false
      architecture: hunyuan-dense
      parameters: 1790000000
      context_length: 262144
      embedding_length: 2048
      quantization: Q4_K_M
      note: 'Translation-only model. No system prompt needed (per Hy-MT2 docs). Official prompt: ''Translate the following text into {target_lang}. Note that you should only output the translated result without any additional explanation:


        {source_text}''. Supports terminology, style, personalization, delimiter preservation, and structured data translation. See https://github.com/Tencent-Hunyuan/Hy-MT2 for all prompt formats.'
  lfm2.5-230m:
    name: LFM2.5 230M
    description: 'Liquid AI''s smallest model — 230M params, ultra-fast tool calling and data extraction. Runs anywhere: CPU, NPU, GPU.'
    tags:
    - tools
    cmd: '${llama_server} --port ${PORT}

      --model ${models_dir}/LFM2.5-230M-Q8_0.gguf

      --jinja

      --fit on

      --fit-target ${vram_margin}

      --fit-ctx ${small_model_min_ctx}

      --ctx-size 32768

      --parallel 4

      --cont-batching

      --temp ${code_temp}

      --top-p ${code_top_p}

      --top-k ${code_top_k}

      --min-p ${code_min_p}

      --repeat-penalty ${code_repeat_penalty}

      --cache-type-k ${small_cache_k}

      --cache-type-v ${small_cache_v}

      --flash-attn on

      --metrics

      '
    filters:
      stripParams: temperature, top_p
      setParamsByID:
        ${MODEL_ID}:
          temperature: 0.3
          top_p: 0.9
    health_check_timeout: 300
    ttl: 60
    capabilities:
      in:
      - text
      out:
      - text
      tools: true
      reranker: false
      context: 32768
    metadata:
      size: ~247 MB (Q8_0)
      context: 32K (extendable to 128K)
      vram_usage: ~0.4GB (model + cache)
      source: LiquidAI/LFM2.5-230M-GGUF (Q8_0)
      kv_cache: q8_0 + q8_0
      features:
        completion: true
        tools: true
        thinking: false
        vision: false
        embedding: false
        insert: false
        audio: false
        image: false
      architecture: lfm2
      parameters: 229693184
      context_length: 128000
      embedding_length: 1024
      quantization: Q8_0
      warning: NOT recommended for math, code generation, or creative writing
  lfm2.5-8b-a1b:
    name: LFM 2.5 8B
    description: Liquid AI MoE hybrid — 8.3B total, 1.5B active per token. Built for fast, reliable tool calling on consumer hardware.
    cmd: '${llama_server} --port ${PORT}

      --model ${models_dir}/lfm2.5-8b-a1b/LFM2.5-8B-A1B-APEX-I-Compact.gguf

      --fit on

      --fit-ctx 16384

      --ctx-size 131072

      --parallel 2

      --no-cont-batching

      --reasoning-format deepseek-legacy

      --reasoning on

      --chat-template-file /home/alchemist/git/ai-dotfiles/llama-swap/templates/lfm2.5-chat-template.jinja

      --temp 0.2

      --top-p 0.8

      --top-k 40

      --min-p 0.15

      --repeat-penalty 1.05

      --cache-type-k q8_0

      --cache-type-v q4_0

      --flash-attn on

      --n-predict -1

      --jinja

      --metrics

      '
    filters:
      stripParams: temperature, top_p
      setParamsByID:
        ${MODEL_ID}:
          temperature: 0.2
          min_p: 0.15
        ${MODEL_ID}:think:
          chat_template_kwargs:
            enable_thinking: true
          temperature: 0.3
    ttl: 60
    health_check_timeout: 300
    capabilities:
      in:
      - text
      out:
      - text
      tools: true
      reranker: false
      context: 131072
    metadata:
      size: ~4.2 GB (APEX I-Compact)
      context: 128K
      vram_usage: ~5.2GB VRAM (4.2 GB model + KV)
      source: mudler/LFM2.5-8B-A1B-APEX-GGUF
      kv_cache: q8_0 + q4_0 hadamard
      features:
        completion: true
        tools: true
        thinking: true
        vision: false
        embedding: false
        insert: false
        audio: false
        image: false
      architecture: lfm2moe
      parameters: 8470000000
      context_length: 128000
      embedding_length: 2048
      quantization: APEX I-Compact
      note: 'MoE 1.5B active, hybrid conv+GQA. Reasoning + tool calling. APEX I-Compact: 13% smaller than Q4_0, imatrix v1.3 calibrated. Uses upstream (BeeLlama segfaults on LFM2.5). GGUF has empty chat_template — requires --chat-template-file + --reasoning-format deepseek.'
  lfm2.5-vl-1.6b-extract:
    name: LFM2.5-VL 1.6B Extract
    description: Vision-language model specialized in structured extraction. Pass an image and a list of fields, get back a JSON object. No parsing layer needed.
    cmd: '${llama_server} --port ${PORT}

      --model ${models_dir}/LFM2.5-VL-1.6B-Extract-Q4_K_M.gguf

      --jinja

      --mmproj ${models_dir}/mmproj-LFM2.5-VL-1.6B-Extract-F16.gguf

      --media-path ${media_path}

      --fit on

      --fit-target ${vram_margin}

      --fit-ctx ${small_model_min_ctx}

      --ctx-size ${small_model_max_ctx}

      --temp 0.1

      --top-p 0.9

      --top-k 40

      --min-p 0.15

      --repeat-penalty 1.3

      --reasoning off

      --cache-type-k f16

      --cache-type-v f16

      --flash-attn on

      --metrics

      '
    health_check_timeout: 300
    ttl: 60
    capabilities:
      in:
      - text
      - image
      out:
      - text
      tools: false
      reranker: false
      context: 131072
    metadata:
      size: 0.70 GB (Q4_K_M) + 0.82 GB mmproj
      context: 32K (dynamic)
      vram_usage: ~2.7GB
      source: LiquidAI/LFM2.5-VL-1.6B-Extract-GGUF
      kv_cache: f16
      features:
        completion: true
        tools: false
        thinking: false
        vision: true
        embedding: false
        insert: false
        audio: false
        image: false
      architecture: lfm2
      parameters: 1600000000
      context_length: 128000
      embedding_length: 2048
      quantization: Q4_K_M
      note: Structured extraction VLM — returns JSON instead of free-form text. lfm2 arch requires upstream llama.cpp (not ik).
  lfm2.5-vl-450m-extract:
    name: LFM2.5-VL 450M Extract
    description: Ultra-light vision-language model for structured extraction. Returns JSON from image + field list. Edge-ready.
    cmd: '${llama_server} --port ${PORT}

      --model ${models_dir}/LFM2.5-VL-450M-Extract-Q4_K_M.gguf

      --jinja

      --mmproj ${models_dir}/mmproj-LFM2.5-VL-450M-Extract-F16.gguf

      --media-path ${media_path}

      --fit on

      --fit-target ${vram_margin}

      --fit-ctx ${small_model_min_ctx}

      --ctx-size ${small_model_max_ctx}

      --temp 0.1

      --top-p 0.9

      --top-k 40

      --min-p 0.15

      --repeat-penalty 1.3

      --reasoning off

      --cache-type-k f16

      --cache-type-v f16

      --flash-attn on

      --metrics

      '
    health_check_timeout: 300
    ttl: 60
    capabilities:
      in:
      - text
      - image
      out:
      - text
      tools: false
      reranker: false
      context: 131072
    metadata:
      size: 0.22 GB (Q4_K_M) + 0.18 GB mmproj
      context: 32K (dynamic)
      vram_usage: ~1.0GB
      source: LiquidAI/LFM2.5-VL-450M-Extract-GGUF
      kv_cache: f16
      features:
        completion: true
        tools: false
        thinking: false
        vision: true
        embedding: false
        insert: false
        audio: false
        image: false
      architecture: lfm2
      parameters: 450000000
      context_length: 128000
      embedding_length: 2048
      quantization: Q4_K_M
      note: Ultra-light Extract VLM — 450M params, returns JSON. lfm2 arch requires upstream llama.cpp (not ik).
  nomic-embed-text-v2-moe:
    name: Nomic Embed v2
    description: Multilingual MoE text embedding model that excels at multilingual retrieval, supporting ~100 languages with state-of-the-art performance at the 300M parameter scale.
    env:
    - CUDA_VISIBLE_DEVICES=
    cmd: '${llama_server} --port ${PORT}

      --model ${models_dir}/nomic-embed-text-v2-moe-Q4_K_M.gguf

      --embeddings

      --pooling mean

      --ctx-size 512

      --cont-batching

      --cache-type-k f16

      --cache-type-v f16

      -ngl 0

      --metrics

      '
    ttl: 300
    capabilities:
      in:
      - text
      out: []
      tools: false
      reranker: false
      context: 512
    metadata:
      architecture: nomic-bert
      parameters: 475000000
      context_length: 512
      embedding_length: 768
      quantization: Q4_K_M
      size: ~0.33 GB (Q4_K_M)
      vram_usage: ~0.1GB (CPU-only, -ngl 0, CUDA ctx overhead only)
      source: nomic-ai/nomic-embed-text-v2-moe-GGUF (Q4_K_M)
      kv_cache: f16
      features:
        completion: false
        tools: false
        thinking: false
        vision: false
        embedding: true
        insert: false
        audio: false
        image: false
      note: 'Embedding-only model. Use /v1/embeddings endpoint. Prefix inputs with ''search_query: '' or ''search_document: ''. 100+ languages. Matryoshka → 256 dims.'
  north-mini-code:
    name: North Mini Code
    description: Cohere's first open-source agentic coding model. 30B MoE with only 3B active params, optimized for code generation, agentic software engineering, and terminal tasks.
    cmd: '${ik_llama_server} --port ${PORT}

      --model ${models_dir}/north-mini-code-q4km/North-Mini-Code-1.0-Q4_K_M.gguf

      --jinja

      --reasoning on

      --fit

      --fit-margin 256

      --parallel-tool-calls

      --ctx-size 65536

      --temp 1.0

      --top-p 0.95

      --repeat-penalty 1.0

      --parallel 1

      --cache-type-k ${large_cache_k}

      --cache-type-v ${large_cache_v}

      --k-cache-hadamard

      --v-cache-hadamard

      --flash-attn auto

      --defer-experts

      --metrics

      '
    filters:
      stripParams: temperature, top_p
      setParamsByID:
        ${MODEL_ID}:
          chat_template_kwargs:
            enable_thinking: false
          temperature: 1.0
          top_p: 0.95
        ${MODEL_ID}:think:
          chat_template_kwargs:
            enable_thinking: true
          temperature: 1.0
          top_p: 0.95
    ttl: 300
    health_check_timeout: 300
    capabilities:
      in:
      - text
      out:
      - text
      tools: true
      reranker: false
      context: 65536
    metadata:
      size: ~18.6 GB (Q4_K_M)
      context: 16K-65K (dynamic with --fit)
      vram_usage: ~5.6GB VRAM + ~14GB RAM (ik pinned memory, MoE offload)
      source: Arki05/North-Mini-Code-1.0-GGUF
      kv_cache: q4_0 + hadamard
      features:
        completion: true
        tools: true
        thinking: true
        vision: false
        embedding: false
        insert: false
        audio: false
        image: false
      architecture: cohere2moe
      parameters: 30484340938
      context_length: 262144
      embedding_length: 2048
      quantization: Q4_K_M
  ornith-1.0-9b:
    name: Ornith 1.0 9B
    description: Ornith-1.0 is a family of open-source LLMs specialized for agentic coding, trained with a novel self-scaffolding reinforcement learning approach that jointly optimizes the scaffold and the solution.
    cmd: '${ik_llama_server} --port ${PORT}

      --model ${models_dir}/ornith-1.0-9b/ornith-1.0-9b-Q4_K_M.gguf

      --fit

      --fit-margin 512

      --jinja

      --parallel 2

      --parallel-tool-calls

      --ctx-size ${small_model_max_ctx}

      --temp 1.0

      --top-p 1.0

      --top-k ${default_top_k}

      --min-p ${default_min_p}

      --repeat-penalty ${default_repeat_penalty}

      --reasoning on

      --cache-type-k q8_0

      --cache-type-v q4_0

      --k-cache-hadamard

      --v-cache-hadamard

      --flash-attn auto

      --metrics

      '
    filters:
      stripParams: temperature, top_p
      setParamsByID:
        ${MODEL_ID}:
          chat_template_kwargs:
            enable_thinking: false
          temperature: 1.0
          top_p: 1.0
        ${MODEL_ID}:think:
          chat_template_kwargs:
            enable_thinking: true
          temperature: 1.0
          top_p: 1.0
    ttl: 60
    health_check_timeout: 300
    capabilities:
      in:
      - text
      out:
      - text
      tools: true
      reranker: false
      context: 262144
    metadata:
      size: ~5.63 GB (Q4_K_M)
      context: 64K-128K (dynamic)
      vram_usage: ~5.2GB VRAM (text-only, ik with hadamard cache)
      source: deepreinforce-ai/Ornith-1.0-9B-GGUF
      kv_cache: q8_0 + q4_0 hadamard
      features:
        completion: true
        tools: true
        thinking: true
        vision: false
        embedding: false
        insert: false
        audio: false
        image: false
      architecture: qwen35
      parameters: 9000000000
      context_length: 262144
      embedding_length: 4096
      quantization: Q4_K_M
      note: Post-trained Qwen 3.5 9B Dense + Gemma 4. Self-scaffolding RL for agentic coding. Identity bleeds as 'trained by Google' — use system prompt to override.
  qwen3-asr-0.6b:
    name: Qwen3-ASR 0.6B
    description: Lightweight multilingual ASR model for speech-to-text transcription. Supports multiple languages with high accuracy. Audio input via qwen3a preprocessor.
    tags:
    - audio
    cmd: '${llama_server} --port ${PORT}

      --model ${models_dir}/Qwen3-ASR-0.6B-Q8_0.gguf

      --mmproj ${models_dir}/mmproj-Qwen3-ASR-0.6B-Q8_0.gguf

      --jinja

      --fit on

      --fit-target 768

      --fit-ctx ${small_model_min_ctx}

      --ctx-size ${small_model_max_ctx}

      --parallel 4

      --cont-batching

      --temp ${default_temp}

      --top-p ${default_top_p}

      --top-k ${default_top_k}

      --min-p ${default_min_p}

      --repeat-penalty ${default_repeat_penalty}

      --cache-type-k ${small_cache_k}

      --cache-type-v ${small_cache_v}

      --flash-attn on

      --metrics

      '
    health_check_timeout: 300
    ttl: 60
    capabilities:
      in:
      - text
      - audio
      out:
      - text
      tools: false
      reranker: false
      context: 131072
    metadata:
      size: ~1.0 GB (Q8_0 model 768MB + mmproj 205MB)
      context: 32K-128K (dynamic)
      vram_usage: ~1.5GB (model + mmproj + cache)
      source: ggml-org/Qwen3-ASR-0.6B-GGUF (Q8_0)
      kv_cache: q8_0 + attn_rot
      features:
        completion: true
        tools: false
        thinking: false
        vision: false
        embedding: false
        insert: false
        audio: true
        image: false
      architecture: qwen3vl
      parameters: 600000000
      context_length: 131072
      embedding_length: 896
      quantization: Q8_0
      caveats:
      - ASR-only model — no audio generation capability
      - Multilingual ASR support (better than English-only models)
      - Very lightweight, can run alongside other small models
  qwen3-tts:
    name: Qwen3-TTS 0.6B
    description: Text-to-speech with voice cloning. 8 Portuguese voices. OpenAI-compatible /v1/audio/speech endpoint with voice profiles from voiceclone-tui.
    tags:
    - tts
    proxy: http://127.0.0.1:8880
    checkEndpoint: /v1/audio/voices
    cmd: '${env.HOME}/projects/ai/tts-api/.venv/bin/python ${env.HOME}/projects/ai/tts-api/tts_server.py --port 8880 --host 127.0.0.1

      '
    cmdStop: pkill -f tts_server.py
    health_check_timeout: 300
    ttl: 300
    capabilities:
      in:
      - text
      out:
      - audio
      tools: false
      reranker: false
      context: 0
    metadata:
      size: ~600MB (Qwen3-TTS-12Hz-0.6B-Base, torch-direct)
      context: N/A (TTS generation, not LLM context)
      vram_usage: ~2GB VRAM (torch-direct, separate GPU allocation)
      source: Qwen/Qwen3-TTS-12Hz-0.6B-Base (HuggingFace)
      kv_cache: N/A
      features:
        completion: false
        tools: false
        thinking: false
        vision: false
        embedding: false
        insert: false
        audio: true
        image: false
      architecture: qwen3-tts
      parameters: 600000000
      context_length: 0
      embedding_length: 0
      quantization: torch-direct (no GGUF)
      caveats:
      - External FastAPI server on port 8880 (not llama.cpp)
      - Must install qwen_tts and dependencies in ~/projects/ai/tts-api/.venv
      - Voice profiles loaded from ~/.config/voiceclone-tui/voices.json
      - Supports voice cloning with reference audio samples
      - Portuguese (pt-BR) voices with accent control
  qwen3.5-4b-abliterated:
    name: Qwen3.5 4B Abliterated
    description: Qwen 3.5 is a family of open-source multimodal models that delivers exceptional utility and performance. Abliterated variant with refusal behaviors removed for adversarial testing.
    cmd: '${ik_llama_server} --port ${PORT}

      --model ${models_dir}/Qwen3.5-4B-abliterated.i1-Q4_K_M.gguf

      --jinja

      --parallel 2

      --parallel-tool-calls

      --fit

      --fit-margin 512

      --ctx-size 131072

      --temp ${default_temp}

      --top-p ${default_top_p}

      --top-k ${default_top_k}

      --min-p ${default_min_p}

      --repeat-penalty ${default_repeat_penalty}

      --reasoning on

      --cache-type-k q8_0

      --cache-type-v q4_0

      --k-cache-hadamard

      --v-cache-hadamard

      --flash-attn auto

      --metrics

      '
    filters:
      stripParams: temperature, top_p
      setParamsByID:
        ${MODEL_ID}:
          chat_template_kwargs:
            enable_thinking: false
          temperature: 0.7
          top_p: 0.9
        ${MODEL_ID}:think:
          chat_template_kwargs:
            enable_thinking: true
          temperature: 0.6
          top_p: 0.9
    ttl: 60
    health_check_timeout: 300
    capabilities:
      in:
      - text
      out:
      - text
      tools: true
      reranker: false
      context: 131072
    metadata:
      architecture: qwen35
      parameters: 4210000000
      context_length: 262144
      embedding_length: 2560
      quantization: i1-Q4_K_M
      size: ~2.71 GB (i1-Q4_K_M)
      context: 64K-128K (dynamic)
      vram_usage: ~3.5GB VRAM (model + hadamard cache)
      source: mradermacher/Qwen3.5-4B-abliterated-i1-GGUF (i1-Q4_K_M)
      kv_cache: q8_0 + q4_0 hadamard
      features:
        completion: true
        tools: true
        thinking: true
        vision: false
        embedding: false
        insert: false
        audio: false
        image: false
      note: Abliterated variant for Sprachspiel adversarial testing — no refusal. Switched from Bee to ik — i1 format causes Bee slash bug.
hooks:
  on_startup:
    preload: []
matrix:
  vars:
    q4: qwen3.5-4b
    q4a: qwen3.5-4b-abliterated
    g2: gemma4-e2b
    g2q: gemma4-e2b
    lfm230: lfm2.5-230m
    lfm8: lfm2.5-8b-a1b
    vl: lfm2.5-vl-450m
    vlx: lfm2.5-vl-1.6b-extract
    vle: lfm2.5-vl-450m-extract
    ocr: glm-ocr
    cpv: minicpm-v-4.6
    q9: qwen3.5-9b
    g4: gemma4-e4b
    qw36: qwen3.6-35b-a3b
    orn35: ornith-1.0-35b
    a1: agents-a1-35b
    glm: glm-4.7-flash
    nex: nex-n2-mini
    nmc: north-mini-code
    hmt: hy-mt2-1.8b
    asr: qwen3-asr-0.6b
    ttsext: qwen3-tts
    nomic: nomic-embed-text-v2-moe
  sets:
    small_vision: (q4 | q4a | g2 | g2q | lfm230 | lfm8) & vl & nomic
    small_embed: (q4 | q4a | g2 | g2q | lfm230 | lfm8) & nomic
    small_only: q4 | q4a | g2 | g2q | lfm230 | lfm8
    vision_only: vl | vlx | vle | ocr | cpv
    vision_embed: (vl | vlx | vle | ocr | cpv) & nomic
    medium: (q9 | g4) & nomic
    heavy: (qw36 | orn35 | nmc | a1 | glm | nex) & nomic
    translate_embed: hmt & nomic
    translate_only: hmt
    tts_external: ttsext
    embed_only: nomic
  evict_costs:
    qw36: 10
    orn35: 9
    a1: 9
    glm: 9
    nex: 9
    nmc: 9
    q9: 4
    g4: 4
    ttsext: 4
    vl: 3
    vlx: 3
    vle: 1
    ocr: 3
    cpv: 3
    q4: 2
    q4a: 2
    lfm8: 2
    asr: 2
    nomic: 1
    g2: 1
    g2q: 1
    lfm230: 1
    hmt: 1
