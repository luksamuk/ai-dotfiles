# -------------------------------------------
# QWEN3.5-4B - Bee BACKEND (DFlash speculative decoding)
# Dense 4B params, i1-Q4_K_M ~2.6GB + DFlash drafter Q4_K_M ~313MB.
# DFlash: block diffusion drafter reads target hidden states, drafts
# multiple tokens in parallel. 1.64x speedup over ik+MoQ (86 vs 53 tok/s).
# BeeLlama.cpp backend with GPU cross-attention ring (5 layers x 512 slots).
# Adaptive draft-max: profit controller, n_max=8, ~71% acceptance rate.
# MoQ-3.75 incompatible with Bee (missing tokenizer merges).
# Tool calling: works natively via Jinja (no --parallel-tool-calls flag in Bee).
# Thinking: reasoning_content field, separate from content.
# Dual mode: base (chat, no think) and :think (reasoning)
# -------------------------------------------
"qwen3.5-4b":
  name: "Qwen3.5 4B"
  description: "Qwen 3.5 is a family of open-source multimodal models that delivers exceptional utility and performance, integrating breakthroughs in multimodal learning, architectural efficiency, and reinforcement learning."
  cmd: |
    ${bee_server} --port ${PORT}
    --model ${models_dir}/Qwen3.5-4B-abliterated.i1-Q4_K_M.gguf
    --spec-draft-model ${models_dir}/qwen35-4b-dflash-Q4_K_M.gguf
    --spec-type dflash
    --spec-draft-ngl all
    --spec-dflash-cross-ctx 512
    --spec-draft-n-max 8
    --spec-branch-budget 0
    --jinja
    --parallel 1
    -ngl all
    --ctx-size 32768
    --temp ${default_temp}
    --top-p ${default_top_p}
    --top-k ${default_top_k}
    --min-p ${default_min_p}
    --repeat-penalty ${default_repeat_penalty}
    --reasoning on
    --cache-type-k q4_0
    --cache-type-v q4_0
    --flash-attn on
    --metrics
  filters:
    stripParams: "temperature, top_p"
    setParamsByID:
      "${MODEL_ID}":
        chat_template_kwargs:
          enable_thinking: false
        temperature: 0.7
        top_p: 0.9
      "${MODEL_ID}:think":
        chat_template_kwargs:
          enable_thinking: true
        temperature: 0.6
        top_p: 0.9
  ttl: 60
  health_check_timeout: 300
  capabilities:
    in: ['text']
    out: ['text']
    tools: true
    reranker: false
    context: 32768
  metadata:
    size: "~2.9 GB (Q4_K_M + DFlash drafter)"
    context: "32K (DFlash max on 6GB: 4782 MiB. 98K needs 5678 MiB — doesn't fit even alone)"
    vram_usage: "~4.8GB VRAM (model 2.6GB + drafter 313MB + DFlash ring + q4_0 cache 32K)"
    source: "Qwen3.5-4B-abliterated i1-Q4_K_M + Anbeeld/Qwen3.5-4B-DFlash-GGUF"
    kv_cache: "q4_0 (BeeLlama.cpp)"
    features:
      completion: true
      tools: true
      thinking: true
      vision: false
      embedding: false
      insert: false
      audio: false
      image: false
    architecture: qwen35
    parameters: 4210000000
    context_length: 32768
    embedding_length: 2560
    quantization: i1-Q4_K_M + DFlash Q4_K_M
    note: "DFlash speculative decoding via BeeLlama. 1.64x faster than ik+MoQ (86 vs 53 tok/s). Flat DFlash (branch-budget=0), adaptive draft-max (profit controller). Tool calling + thinking tested OK."